<div>
  <%= form_with model: @user, class: 'login__form', url: login_path do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit "Login", class: 'btn btn-primary' %>
  <% end %>
</div
